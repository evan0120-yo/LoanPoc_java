# 貸款系統模組架構分析

## 1. 模組職責總覽

| 模組 | 角色定位 | 對外 API？ | 核心職責 |
|------|---------|-----------|---------|
| **origin** | 審核官 + 定價師 | ✅ 對外入口 | 接收申請、決策引擎、額度計算、利率定價 |
| **bureau** | 偵探部門 | ❌ 內部 | 對接第三方徵信、標準化報告 |
| **loancore** | 總指揮中心 | ⚠️ Saga 協調 | **統一訂單存儲**、狀態機管理、跨模組協調 |
| **sign** | 法務部 | ❌ 內部 | KFS 文件、Penny Drop、e-Sign、e-NACH |
| **pay** | 銀行櫃台 | ❌ 內部 | 放款、收款、Webhook 處理 |
| **ledger** | 會計部門 | ❌ 內部 | 複式簿記、計息、餘額管理 |
| **knockoff** | 分錢機 | ❌ 內部 | 還款分配 (Waterfall)、溢繳處理 |
| **callect** | 催債部門 | ❌ 內部 | 分桶催收、PTP 追蹤、代理管理 |
| **loancron** | 值夜班的人 | ❌ 內部 | 排程任務、日切、逾期檢測 |
| **lsp** | 轉介部門 | ❌ 內部 | 瀑布流導流、合作商管理 |

---

## 2. 關鍵問題：誰是對外 API 入口？

根據 readme 分析，**目前沒有明確的對外入口模組**。

### 現有 readme 的設計
```
loancore.readme 說：
    「loancore → 各模組：同步呼叫或 Command MQ」
    「各模組 → loancore：MQ 事件通知」

origin.readme 說：
    「loancore → origin：同步呼叫（審核是關鍵路徑）」
```

這暗示 **loancore 是協調者**，但沒說清楚**誰接收前端請求**。

---

## 3. 建議架構：統一資料來源

### 核心設計決策

> [!IMPORTANT]
> **所有貸款流程統一用 `loancore.LoanOrder` 記錄**
> - origin 收到申請後**立刻**在 loancore 建單
> - 無論最後走自己做、拒絕、還是導流，都在同一張表追蹤
> - origin 不存 LoanApplication，只負責決策邏輯

| 考量 | 說明 |
|------|------|
| **單一資料來源** | 查用戶貸款狀態只需查 `LoanOrder` |
| **簡化架構** | 不需要 origin 和 loancore 兩邊同步資料 |
| **完整狀態機** | `LoanStateEnum` 已涵蓋所有狀態 (PENDING → REJECTED/LSP_ROUTING/CLOSED) |

### 建議的流程

#### 階段一：用戶申請 (同步)

```
前端 App
    │
    │ POST /origin/apply
    ▼
┌──────────┐         ┌──────────────┐
│  origin  │────────▶│   loancore   │ 建單 status=PENDING
└────┬─────┘         └──────────────┘
     │
     │ 返回 orderId
     ▼
  前端顯示「審核中」
```

#### 階段二：排程審核 (異步，由 loancron 觸發)

```
┌──────────┐         ┌──────────────┐
│ loancron │────MQ──▶│   loancore   │ 1. 收到觸發訊號
└──────────┘         │              │ 2. 自己查詢 PENDING 訂單
                     │              │ 3. 呼叫 bureau
                     └──────┬───────┘
                            │
                     ┌──────▼───────┐
                     │    bureau    │ 收集資料 (CIBIL, BSA...)
                     └──────┬───────┘
                            │
                     ┌──────▼───────┐
                     │   loancore   │ 3. 更新 BUREAU_CHECK
                     │              │ 4. 呼叫 origin 做決策
                     └──────┬───────┘
                            │
                     ┌──────▼───────┐
                     │    origin    │ 決策引擎 (回傳結果)
                     └──────┬───────┘
                            │
                     ┌──────▼───────┐
                     │   loancore   │ 5. 更新狀態
                     └──────┬───────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
 ┌────────────┐      ┌────────────┐      ┌────────────┐
 │  REJECTED  │      │OFFER_READY │      │LSP_ROUTING │
 │  (終態)    │      └─────┬──────┘      └─────┬──────┘
 └────────────┘            │                   │
                           │                   ▼
                           │            ┌──────────────┐
                           │            │   loancore   │ 觸發 lsp
                           │            └──────┬───────┘
                           │                   │
                           │            ┌──────▼───────┐
                           │            │     lsp      │ 導流
                           │            └──────────────┘
                           │
                    ┌──────▼───────┐
                    │   loancore   │ 6. 觸發 sign
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │     sign     │ 簽約流程
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │   loancore   │ 7. 更新 SIGNED
                    │              │ 8. 觸發 pay
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │     pay      │ 放款
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │   loancore   │ 9. 更新 DISBURSED
                    │              │ 10. 觸發 ledger
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │    ledger    │ 記帳
                    └──────────────┘
```

---

## 4. 完整模組互動圖

```
                                 ┌─────────────┐
                                 │   前端 App   │
                                 └──────┬──────┘
                                        │
                   ┌────────────────────┼────────────────────┐
                   │                    │                    │
                   ▼                    ▼                    ▼
            ┌──────────┐         ┌──────────┐         ┌──────────┐
            │  origin  │────────▶│ loancore │         │   pay    │
            │ (申請)   │ 立刻建單 │ (訂單)   │         │ (還款)   │
            └──────────┘         └────┬─────┘         └────┬─────┘
                                      │                    │
                                      ▼                    │
                               ┌──────────┐                │
                               │ loancron │ ← 排程觸發     │
                               └────┬─────┘                │
                                    │                      │
      ┌─────────────────────────────┼──────────────┐       │
      ▼                             ▼              ▼       │
┌─────────┐                   ┌─────────┐    ┌─────────┐   │
│ bureau  │                   │ origin  │    │ callect │   │
│(徵信)   │                   │(決策)   │    │(催收)   │   │
└────┬────┘                   └────┬────┘    └─────────┘   │
     │                             │                       │
     │      ┌──────────────────────┴───────────┐           │
     │      ▼                                  ▼           │
     │ ┌────────┐                        ┌─────────┐       │
     │ │loancore│                        │   lsp   │       │
     │ │(狀態機)│                        │ (導流)  │       │
     │ └───┬────┘                        └─────────┘       │
     │     │                                               │
     │     ▼                                               │
     │ ┌────────┐                                          │
     │ │  sign  │                                          │
     │ │ (簽約) │                                          │
     │ └───┬────┘                                          │
     │     │                                               │
     │     ▼                                               │
     │ ┌────────┐                                          │
     │ │  pay   │ ←────────────────────────────────────────┘
     │ │(放款/收款)│
     │ └───┬────┘
     │     │
     │     ├─────────────────┐
     │     ▼                 ▼
     │ ┌────────┐       ┌─────────┐
     └▶│ ledger │◀──────│knockoff │
       │ (記帳) │       │(沖銷)   │
       └────────┘       └─────────┘
```

---

## 5. 各模組通訊方式

| 來源 | 目標 | 方式 | 原因 |
|------|------|------|------|
| 前端 → origin | origin | **同步 HTTP** | 需要立即回應 |
| origin → loancore | loancore | **MQ (Outbox)** | 可靠建單 |
| loancron → loancore | loancore | **MQ (Outbox)** | 觸發審核流程（loancore 自己查詢 PENDING 訂單）|
| loancore → bureau | bureau | **MQ (Outbox)** | 可靠發送，使用 Claim-and-Process 避免重複 |
| loancore → origin | origin | **同步 HTTP** | 請求決策評估 |
| origin → loancore | loancore | **同步 HTTP** | 回傳決策結果，更新狀態 |
| loancore → lsp | lsp | **MQ** | 非同步導流 |
| loancore → sign | sign | **同步 HTTP** | 關鍵路徑 |
| sign → loancore | loancore | **MQ** | 狀態更新 |
| loancore → pay | pay | **同步 HTTP** | 放款需即時 |
| pay → ledger | ledger | **MQ** | 記帳非同步 |
| pay → knockoff | knockoff | **MQ** | 還款沖銷 |
| knockoff → ledger | ledger | **同步** | 需原子性 |
| loancron → callect | callect | **MQ** | 逾期通知 |
| loancron → ledger | ledger | **MQ** | 計息觸發 |

---

## 6. 建議的 loancron 角色

**loancron 只做觸發，不存業務資料**

| ✅ 正確職責 | ❌ 錯誤職責 |
|------------|------------|
| 定時發觸發訊號 | 查詢其他模組的資料 |
| 日切計息 / 逾期檢測觸發 | 存儲用戶申請資料 |
| 發 MQ 給其他模組（不帶業務資料）| 直接處理業務邏輯 |

### loancron 的排程任務

| 任務 | 觸發時間 | 調用模組 |
|------|---------|---------|
| 處理待審批訂單 | 每 5 分鐘 | loancore (via MQ) |
| 日切計息 | 每日 00:00 | ledger (via MQ) |
| 逾期檢測 | 每日 00:30 | loancore (via MQ) → callect |
| 自動扣款 | 每日 09:00 | pay (via HTTP) |
| 簽約超時檢測 | 每日 02:00 | loancore (via MQ) |

---

## 7. 結論與建議

### Phase 1 開發順序

1. **loancore** - 訂單建立 + 狀態機 (統一資料來源)
2. **origin** - 對外入口 + 決策邏輯 (不存資料)
3. **loancron** - 排程觸發
4. **bureau** - 徵信 (可 Mock)
5. **sign** - 簽約 (可 Mock)
6. **pay** - 放款 (可 Mock)

### 架構決策

| 決策 | 建議 |
|------|------|
| 前端入口 | **origin** |
| 狀態機 | **loancore** |
| 排程 | **loancron** (只觸發，不存業務資料) |
| 資料存儲 | **統一在 loancore** (LoanOrder 從申請到結案) |
